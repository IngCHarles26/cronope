{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".react-router/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "with": ["@cronope/schemas#dev"]
    },
    "lint": {}
  }
}
